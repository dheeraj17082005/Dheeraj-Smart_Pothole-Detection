# Testcontainers Configuration
# On macOS with Colima: export DOCKER_HOST=unix://${HOME}/.colima/default/docker.sock
# On standard Docker Desktop / Linux: Testcontainers automatically detects /var/run/docker.sock
# docker.host=unix:///var/run/docker.sock
